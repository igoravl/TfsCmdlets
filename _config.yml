url: "https://tfscmdlets.dev"
permalink: pretty
exclude: ["vendor", "node_modules/", "*.gemspec", "*.gem", "Gemfile", "Gemfile.lock", "package.json", "package-lock.json",  "script/", "LICENSE.txt", "lib/", "bin/", "README.md", "Rakefile"]

# Explicitly include files from the .well-known directory
include: [".well-known"]

# Set a path/url to a logo that will be displayed instead of the title
logo: "/assets/images/logo.png"

# Enable or disable the site search
search_enabled: true

# Set the search token separator for hyphenated-word search:
search_tokenizer_separator: /[\s/]+/

# Enable or disable heading anchors
heading_anchors: true

# Aux links for the upper right navigation
aux_links:
  "TfsCmdlets on GitHub":
    - "https://github.com/igoravl/tfscmdlets/"
  "Downloads":
    - "https://github.com/igoravl/tfscmdlets/releases/"

# Footer content appears at the bottom of every page's main content
footer_content: "Copyright &copy; 2014-2025 Igor Abade V. Leite. Distributed by an <a href=\"https://github.com/igoravl/tfscmdlets/tree/master/LICENSE.md\">MIT license.</a>"

# Color scheme currently only supports "dark" or nil (default)
color_scheme: nil

plugins:
  - jekyll-seo-tag
  - jekyll-regex-replace
  - jekyll-postfiles

compress_html:
  clippings: all
  comments: all
  endings: all
  startings: []
  blanklines: false
  profile: false

defaults:
  -
    scope:
      path: "docs/cmdlets"
    values:
      layout: "cmdlet"
      nav_exclude: true

build_info: TfsCmdlets v2.9.1+3877.9, released 2025-06-05
build_info: TfsCmdlets v2.9.2+3932.5, released 2025-07-30

